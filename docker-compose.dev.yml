# Local development overrides. Not used in production.
# Usage: podman compose -f docker-compose.yml -f docker-compose.dev.yml up -d
#
# Binds admin ports to 127.0.0.1 only (not reachable from other machines on the LAN).

services:
  db:
    ports:
      - "127.0.0.1:5432:5432"

  pgadmin:
    image: docker.io/dpage/pgadmin4:latest
    container_name: cueillette-pgadmin
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_DISABLE_POSTFIX: 'true'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
      PGADMIN_CONFIG_WTF_CSRF_ENABLED: 'False'
    ports:
      - "127.0.0.1:5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - ./pgadmin-servers.json:/pgadmin4/servers.json:ro
    depends_on:
      - db
    networks:
      - cueillette-network

  backend:
    ports:
      - "127.0.0.1:8080:8080"
